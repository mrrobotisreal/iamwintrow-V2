type mainReducerAction = 'SET_COOKIE';
type mainReducerState = {
  //
};
type mainReducerType = () => mainReducerState;

const initState: mainReducerState = {};

export const mainReducer = (state: mainReducerState = initState, action: mainReducerAction) => {
  //
};